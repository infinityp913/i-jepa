[project]
name = "i-jepa"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0",
    "datasets==4.4.0",
    "torchvision==0.24.1",
    "matplotlib",
    "tqdm>=4.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
